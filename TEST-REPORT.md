# گزارش بررسی کیف‌تاخیر v1.2.1

## بررسی‌های انجام‌شده در محیط تحویل

- صحت نحوی JavaScript با `node --check`: موفق
- تطبیق تمام شناسه‌های DOM استفاده‌شده در `app.js` با `index.html`: موفق، مورد گمشده صفر
- تطبیق دستی APIهای پنل با Endpointهای ASP.NET Core: موفق
- بررسی جریان Long Polling، ثبت Update ID و مدیریت قطع موقت اتصال
- بررسی جریان ثبت عضو با Reply مدیر، دکمه ثبت عمومی، ورود عضو جدید، خروج عضو و شناسایی کاربران گروه
- بررسی فرمان `/fine200`، افزایش دقیق ۲۰۰,۰۰۰ تومان، انقضای فاکتور قدیمی و کنترل دسترسی مدیر
- بررسی جریان صورت‌حساب شخصی، PreCheckout، SuccessfulPayment، کسر بدهی، افزایش صندوق و اعلان خودکار گروه
- بررسی جلوگیری از پرداخت شخص دیگر، مبلغ نادرست، ارز نادرست، فاکتور منقضی و تراکنش تکراری
- بررسی انقضای فاکتورهای باز پس از تغییر بدهی
- بررسی واکنش‌گرایی رابط در Breakpointهای 1180، 920 و 650 پیکسل
- بررسی وجود لوگو، Favicon، عنوان پروژه و Cache Busting فایل‌های استاتیک

## محدودیت محیط تحویل

در این محیط Docker و .NET SDK نصب نبود؛ بنابراین Build و اتصال واقعی به API بله در همین محیط اجرا نشد. اسکریپت‌های `run.ps1` و `test.ps1` برای Build آفلاین، Health Check و تشخیص خطاهای شناخته‌شده داخل پروژه قرار دارند.

- بررسی مسیر فرمان‌های `/addmember`، `/fine200`، `/debtors`، `/fund`، `/status`، `/commands`، `/bill` و `/pay`
