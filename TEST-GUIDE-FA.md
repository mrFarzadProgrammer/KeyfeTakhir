# راهنمای کامل تست کیف‌تاخیر v1.2.1

این راهنما تمام سناریوهای اصلی بات، پنل مدیریت، ثبت اعضا، بدهی، صورت‌حساب، پرداخت و صندوق را مرحله‌به‌مرحله آزمایش می‌کند.

## 1. آماده‌سازی اولیه

در فایل `.env` این مقادیر باید صحیح باشند:

```env
BALE_BOT_TOKEN=<توکن خام بات>
BALE_PROVIDER_TOKEN=<توکن پرداخت بله>
BALE_BOT_USERNAME=<نام کاربری بات بدون @>
BALE_USE_LONG_POLLING=true
BALE_LONG_POLLING_TIMEOUT_SECONDS=25
BALE_ADMIN_USER_ID=<شناسه عددی مدیر>
BALE_GROUP_CHAT_ID=<شناسه عددی گروه>
ADMIN_USERNAME=admin
ADMIN_PASSWORD=<رمز قوی پنل>
```

بات را داخل گروه اضافه کن و برای عملکرد بهتر، آن را مدیر گروه قرار بده. دسترسی لازم برای خواندن پیام‌ها و ارسال پیام کافی است.

## 2. اجرای نسخه جدید

در پوشه پروژه اجرا کن:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\run.ps1 -Rebuild
```

نسخه جدید پیش از اجرا، کانتینر قدیمی `late-fee-box` را حذف می‌کند ولی Volume اطلاعات را پاک نمی‌کند.

بعد از موفقیت باید این پیام‌ها را ببینی:

```text
KeyfeTakhir started successfully.
Admin panel: http://localhost:8080/admin/
```

سپس تست سلامت خودکار را اجرا کن:

```powershell
.\test.cmd
```

خروجی مورد انتظار:

```text
HTTP 200 OK
Admin page and logo: OK
Unauthenticated API returns 401: OK
No known fatal error was found.
```

## 3. کنترل لاگ زنده

در یک PowerShell جداگانه اجرا کن:

```powershell
docker compose --env-file .env logs -f --tail 100 late-fee-box
```

در تست‌ها نباید پیام‌های `Unhandled exception`، `NU1301` یا خطای تکرارشونده Long Polling ببینی.

## 4. تست راهنمای مدیر

داخل گروه و با حساب مدیر بفرست:

```text
/commands
```

خروجی باید فهرست این فرمان‌ها را نشان دهد:

```text
/addmember
/fine200
/registermembers
/syncmembers
/debtors
/status
/fund
/debt
```

با یک حساب غیرمدیر نیز `/commands` را بفرست. پاسخ مورد انتظار:

```text
این فرمان فقط برای مدیر کیف‌تاخیر فعال است.
```

## 5. تست ثبت عضو با Reply مدیر

1. از یک حساب عضو، یک پیام عادی داخل گروه بفرست.
2. با حساب مدیر روی همان پیام Reply بزن.
3. یکی از این موارد را بفرست:

```text
/addmember
ثبت عضو
عضو کن
ثبتش کن
```

پاسخ مورد انتظار:

```text
نام شخص به اعضای کیف‌تاخیر اضافه شد.
بدهی فعلی: 0 تومان
```

پنل را باز کن:

```text
http://localhost:8080/admin/
```

در بخش «اعضا و بدهی‌ها» باید آن شخص را با شناسه بله مشاهده کنی.

تست منفی: `/addmember` را بدون Reply بفرست. بات باید توضیح دهد که فرمان باید در پاسخ به پیام عضو ارسال شود.

## 6. تست ثبت عمومی اعضا با دکمه

داخل گروه و با حساب مدیر بفرست:

```text
/registermembers
```

بات باید یک دکمه با عنوان مشابه زیر نشان دهد:

```text
شروع و ثبت خودکار عضویت
```

عضو گروه:

1. روی دکمه می‌زند.
2. وارد خصوصی بات می‌شود.
3. دکمه Start را می‌زند یا `/start` می‌فرستد.

پاسخ مورد انتظار:

```text
عضویت شما در کیف‌تاخیر ثبت شد.
```

شخص باید در بخش اعضای پنل ظاهر شود.

تست منفی: یک کاربر خارج از گروه در خصوصی بات `/start` بفرستد. بات باید اعلام کند عضو گروه نیست.

## 7. تست ثبت عضو جدید هنگام ورود به گروه

1. یک حساب آزمایشی جدید را به گروه اضافه کن.
2. چند ثانیه صبر کن.
3. پنل مدیریت را Refresh کن.

عضو جدید باید خودکار در فهرست اعضا ظاهر شود.


## 8. تست ثبت جریمه ۲۰۰,۰۰۰ تومانی با Reply مدیر

1. از حساب عضو آزمایشی یک پیام داخل گروه بفرست.
2. مدیر روی همان پیام Reply بزند.
3. مدیر این فرمان را ارسال کند:

```text
/fine200
```

نام‌های جایگزین قابل استفاده:

```text
/penalty200
/جریمه200
جریمه ۲۰۰
جریمه ۲۰۰۰۰۰
```

پاسخ مورد انتظار:

```text
برای نام عضو جریمه ثبت شد.
مبلغ جریمه: ۲۰۰,۰۰۰ تومان
بدهی جدید: ... تومان
```

کنترل‌های تکمیلی:

- در پنل «اعضا و بدهی‌ها» باید بدهی شخص دقیقاً ۲۰۰,۰۰۰ تومان افزایش پیدا کند.
- اجرای دوباره `/fine200` باید ۲۰۰,۰۰۰ تومان دیگر به همان بدهی اضافه کند.
- اجرای فرمان بدون Reply باید رد شود و راهنمای Reply نمایش داده شود.
- اجرای فرمان توسط کاربر غیرمدیر باید با پیام محدودیت دسترسی رد شود.
- اگر قبلاً برای آن شخص فاکتور باز صادر شده باشد، پس از جریمه جدید باید منقضی شود و عضو صورت‌حساب تازه بگیرد.

## 9. تست ثبت بدهی در پنل

1. وارد پنل مدیریت شو.
2. بخش «اعضا و بدهی‌ها» را باز کن.
3. عضو آزمایشی را پیدا کن.
4. مبلغ بدهی را مثلاً `200000` تومان تنظیم کن.
5. ذخیره کن.

در پنل باید بدهی همان عضو `200,000 تومان` شود.

نکته: ورودی پنل بر اساس واحد نمایشی تنظیم‌شده پروژه است؛ خروجی پنل و پیام‌های بات را با همان واحد بررسی کن.

## 10. تست فهرست بدهکاران

داخل گروه و با حساب مدیر بفرست:

```text
/debtors
```

خروجی باید شامل موارد زیر باشد:

```text
تعداد بدهکاران
جمع کل بدهی
نام هر بدهکار و مبلغ بدهی او
```

یک حساب غیرمدیر نیز `/debtors` بفرستد؛ بات باید دسترسی را رد کند.

## 11. تست گزارش کلی وضعیت

داخل گروه بفرست:

```text
/status
```

خروجی مورد انتظار:

```text
تعداد اعضای فعال
تعداد افراد بدهکار
کل بدهی باز
فهرست بدهکاران
```

## 12. تست گزارش صندوق

داخل گروه بفرست:

```text
/fund
```

قبل از پرداخت، اعداد زیر را یادداشت کن:

```text
موجودی فعلی
جمع جریمه‌های پرداخت‌شده
تعداد پرداخت موفق
```

بعد از تست پرداخت، دوباره همین فرمان را اجرا خواهی کرد.

## 13. تست صورت‌حساب شخصی عضو

عضو بدهکار در گفت‌وگوی خصوصی بات یکی از این فرمان‌ها را بفرستد:

```text
/bill
/pay
/mydebt
بدهی من
صورت‌حساب من
پرداخت بدهی
```

بات ابتدا عضویت شخص را در گروه بررسی می‌کند و سپس باید نمایش دهد:

```text
نام شخص
مبلغ بدهی فعلی
اعتبار 30 دقیقه‌ای فاکتور
کارت رسمی پرداخت بله
```

کارت پرداخت فقط باید توسط همان حساب بدهکار قابل پرداخت باشد.

## 14. تست فرمان صورت‌حساب داخل گروه

عضو داخل گروه بفرستد:

```text
/bill
```

برای حفظ حریم خصوصی، بات نباید جزئیات بدهی را عمومی کند. باید دکمه ورود به خصوصی بات را نمایش دهد. عضو دکمه را می‌زند و سپس در خصوصی `/bill` را می‌فرستد.

## 15. تست مشاهده بدهی شخص توسط مدیر

مدیر روی پیام عضو بدهکار Reply می‌زند و می‌فرستد:

```text
/debt
```

بات باید بدهی او را نمایش دهد. در صورت فعال بودن Provider Token، کارت پرداخت نیز صادر می‌شود.

## 16. تست امنیت فاکتور

فاکتور شخص اول را با حساب شخص دوم باز کن و برای پرداخت اقدام کن.

پرداخت باید رد شود و پیام مشابه زیر نمایش داده شود:

```text
این فاکتور فقط توسط شخص بدهکار قابل پرداخت است.
```

## 17. تست پرداخت کامل

پیش از پرداخت این سه مقدار را یادداشت کن:

```text
بدهی عضو در پنل
موجودی صندوق در پنل
خروجی /fund در گروه
```

عضو فاکتور خود را کامل پرداخت کند.

پس از پرداخت موفق، در خصوصی باید پیامی شامل این موارد دریافت کند:

```text
پرداخت با موفقیت ثبت شد
مبلغ پرداخت‌شده
مانده بدهی
```

در گروه باید خودکار پیامی مشابه زیر منتشر شود:

```text
پرداخت جدید در کیف‌تاخیر
پرداخت‌کننده
مبلغ پرداخت
مانده بدهی فرد
موجودی جدید صندوق
```

بدون اینکه مدیر فرمانی ارسال کند، گزارش موجودی جدید باید در گروه ظاهر شود.

## 18. کنترل نتیجه پرداخت در پنل

پنل را Refresh کن و بررسی کن:

1. بدهی عضو به‌اندازه مبلغ پرداخت کم شده باشد.
2. موجودی صندوق به‌اندازه مبلغ پرداخت افزایش یافته باشد.
3. در بخش پرداخت‌ها، رکورد با وضعیت Paid ثبت شده باشد.
4. شناسه تراکنش پرداخت ذخیره شده باشد.
5. تعداد پرداخت‌های موفق یک واحد افزایش یافته باشد.

سپس در گروه اجرا کن:

```text
/fund
/debtors
/status
```

تمام اعداد باید با پنل هماهنگ باشند.

## 19. تست تسویه کامل

اگر کل بدهی پرداخت شده باشد، عضو دوباره در خصوصی بفرستد:

```text
/bill
```

پاسخ مورد انتظار:

```text
حساب شما تسویه است و بدهی بازی ندارید.
```

نام شخص نیز نباید در خروجی `/debtors` باقی مانده باشد.

## 20. تست تغییر بدهی بعد از صدور فاکتور

1. عضو یک فاکتور با `/bill` دریافت کند ولی پرداخت نکند.
2. مدیر در پنل مبلغ بدهی را تغییر دهد.
3. عضو تلاش کند فاکتور قدیمی را پرداخت کند.

فاکتور قدیمی باید منقضی یا رد شود. عضو باید صورت‌حساب جدید دریافت کند.

## 21. تست خروج عضو از گروه

1. عضو آزمایشی را از گروه خارج کن.
2. در خصوصی بات `/bill` بفرست.

بات باید اعلام کند شخص عضو فعلی گروه نیست و صورت‌حساب صادر نکند. فاکتورهای باز او نیز باید منقضی شوند.

## 22. تست پایداری بعد از Restart

کانتینر را Restart کن:

```powershell
docker restart late-fee-box
```

پس از چند ثانیه:

```powershell
.\test.cmd
```

پنل را باز کن. اعضا، بدهی‌ها، پرداخت‌ها و موجودی صندوق باید حفظ شده باشند؛ چون اطلاعات داخل Volume زیر نگهداری می‌شود:

```text
late_fee_box_stable_data
```

## 23. چک‌لیست نهایی پذیرش

- ثبت عضو با Reply مدیر موفق است.
- ثبت عضو با دکمه و Start موفق است.
- عضو جدید گروه خودکار ثبت می‌شود.
- بدهی از پنل قابل تنظیم است.
- `/debtors` فهرست بدهکاران را نشان می‌دهد.
- `/fund` موجودی صندوق را نشان می‌دهد.
- عضو با `/bill` صورت‌حساب و کارت پرداخت می‌گیرد.
- شخص دیگر نمی‌تواند فاکتور او را پرداخت کند.
- پرداخت موفق بدهی را کم می‌کند.
- پرداخت موفق صندوق را افزایش می‌دهد.
- بعد از پرداخت، گزارش جدید صندوق خودکار در گروه منتشر می‌شود.
- پنل و گزارش‌های گروه اعداد یکسان دارند.
- اطلاعات بعد از Restart حفظ می‌شوند.
